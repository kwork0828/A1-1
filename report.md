# 과제 1 결과 보고서
## 나만의 프롬프트 관리 프로그램 (Prompt Manager)

| 항목 | 내용 |
|---|---|
| 과제명 | AI 프롬프트 관리 콘솔 프로그램 제작 및 Git/GitHub 버전 관리 |
| 저장소 URL | https://github.com/kwork0828/A1-1 |
| 주요 파일 | `prompt_manager.py`, `README.md`, `report.md`, `.gitignore`, `images/` |
| 개발 환경 | Python 3.14.7 / Visual Studio Code / Windows |
| 버전 관리 | Git / GitHub |
| 외부 라이브러리 | 없음 (파이썬 표준 문법만 사용) |
| 총 커밋 수 | **17건** (기능 단위 분할 커밋) |
| 브랜치 | `main`, `feature/prompt-list` — 원격 반영 완료, 병합 완료 |

---

## 1. 기획 의도

생성형 AI를 사용할 때 잘 작동한 프롬프트는 재사용 가치가 높습니다. 그러나 실제로는 메모장, 채팅 기록, 문서 등에 흩어져 저장되어 정작 다시 쓰려 할 때 찾지 못하는 일이 반복됩니다.

이 프로그램은 프롬프트를 한곳에 모아 **용도별로 분류**하고, **키워드로 검색**하며, 자주 쓰는 것은 **즐겨찾기로 표시**할 수 있도록 설계했습니다. 외부 라이브러리 없이 파이썬 표준 문법만 사용해, 어떤 환경에서든 파일 하나만 있으면 바로 실행됩니다.

---

## 2. 용어 정리 — 보고서를 읽기 위한 사전

프로그램과 버전 관리 용어를 먼저 정리합니다. 이후 본문은 이 용어를 기준으로 서술합니다.

### 2-1. 프로그램 관련 용어

| 용어 | 읽는 법 | 뜻 |
|---|---|---|
| 콘솔 프로그램 | 콘솔 | 그림 창 없이 검은 터미널 화면에서 글자만으로 조작하는 프로그램 |
| 리스트(list) | 리스트 | 여러 개를 **순서대로** 담는 상자. `[1번, 2번, 3번]` 형태 |
| 딕셔너리(dict) | 딕셔너리 | **이름표를 붙여** 값을 담는 상자. `{"제목": "...", "내용": "..."}` 형태 |
| 키(key) / 값(value) | 키 / 밸류 | 딕셔너리에서 이름표가 키, 실제 내용이 값 |
| 불리언(bool) | 불리언 | 참(True) 또는 거짓(False) 두 가지만 갖는 자료형 |
| 함수(function) | 펑션 | 반복되는 작업을 이름 붙여 묶어 둔 코드 덩어리 |
| 반복문(while) | 와일 | 조건이 참인 동안 같은 동작을 되풀이하는 구조 |
| 인덱스(index) | 인덱스 | 리스트에서 몇 번째인지 나타내는 번호. 프로그램 내부에서는 0부터 셈 |
| 예외 처리 | — | 잘못된 입력이 들어와도 프로그램이 멈추지 않게 미리 대비하는 것 |

### 2-2. 버전 관리 관련 용어

| 용어 | 읽는 법 | 뜻 |
|---|---|---|
| Git | 깃 | 파일 변경 이력을 단계별로 저장·되돌릴 수 있게 하는 도구 |
| GitHub | 깃허브 | Git 저장소를 인터넷에 올려 공유하는 웹 서비스 |
| 저장소(repository) | 리포지토리 | 프로젝트 파일과 그 변경 이력을 통째로 담는 공간 |
| 커밋(commit) | 커밋 | 변경 내용을 하나의 기록 단위로 확정 저장하는 행위 |
| 브랜치(branch) | 브랜치 | 원본을 건드리지 않고 따로 작업하기 위한 갈라진 작업 줄기 |
| 병합(merge) | 머지 | 갈라져 작업한 브랜치를 다시 원래 줄기에 합치는 것 |
| 푸시(push) / 풀(pull) | 푸시 / 풀 | 내 컴퓨터 → 인터넷 올리기 / 인터넷 → 내 컴퓨터 내려받기 |
| 클론(clone) | 클론 | 인터넷에 있는 저장소를 내 컴퓨터로 복제해 오는 것 |
| Fast-forward | 패스트포워드 | 충돌 없이 원격 내용을 그대로 이어붙인 병합 방식 |

---

## 3. 개발 환경 구성 과정

### 3-1. 설치 및 설정

Python 공식 사이트에서 설치 파일을 받아 **"Add python.exe to PATH"** 옵션을 체크한 뒤 설치했습니다. 이 옵션을 체크하지 않으면 터미널에서 `python` 명령을 인식하지 못합니다. 이어서 VSCode에 Python 확장을 설치하고, Git 사용자 정보를 등록했습니다.

```bash
python --version          # Python 3.14.7
git --version
git config --global user.name "..."
git config --global user.email "..."
git config --global --list
```

Git 사용자 정보는 커밋에 기록되는 작성자 정보이므로, 설정하지 않으면 커밋 자체가 거부됩니다. GitHub 계정과 VSCode를 연동해 터미널에서 별도 로그인 없이 `push` 가 가능하도록 했습니다.

> 📎 **증빙 화면** — `images/01`, `images/02-연동.png`, `images/03.python version.png`
> Python·Git 버전 출력, `git config` 목록, VSCode의 GitHub 로그인 상태

### 3-2. 저장소 생성과 clone

GitHub 웹에서 `A1-1` 저장소를 먼저 만든 뒤, 이를 로컬 컴퓨터로 복제해 작업 폴더를 마련했습니다. 즉 `clone` 이 이 프로젝트의 출발점입니다.

```bash
cd C:\Users\user\Desktop
git clone https://github.com/kwork0828/A1-1.git
cd A1-1
```

복제된 폴더는 이미 원격 저장소와 연결된 상태이므로, 별도의 `remote add` 없이 바로 `push` 할 수 있습니다. 이후 모든 작업은 `C:\Users\user\Desktop\A1-1` 경로에서 진행했습니다.

`.gitignore` 파일을 추가해 `__pycache__/`, `*.pyc`, `.vscode/` 등 버전 관리가 불필요한 파일을 제외했습니다. 이 파일이 없으면 파이썬이 자동 생성하는 캐시 폴더까지 커밋 대상으로 잡혀 이력이 지저분해집니다.

---

## 4. 프로그램 구조

### 4-1. 전체 흐름

```text
프로그램 시작
   └─ main() : 무한 반복 루프
        ├─ show_menu()        메뉴 화면 출력
        ├─ 사용자 번호 입력
        ├─ 1~7 → 해당 기능 함수 실행 → 다시 메뉴로
        ├─ 0   → 종료 메시지 출력 후 루프 탈출
        └─ 그 외 → 안내 메시지 출력 후 다시 메뉴로
```

메뉴를 한 번 처리하고 끝나는 것이 아니라, `while True` 반복문으로 감싸 **종료를 선택할 때까지 계속 사용 가능**하도록 구성했습니다.

### 4-2. 함수 구성

기능별로 함수를 나누고, 여러 기능에서 반복되는 부분은 공용 함수로 분리했습니다.

| 구분 | 함수 | 역할 |
|---|---|---|
| 공용 | `print_line()` | 구분선 출력 |
| 공용 | `print_prompt_line()` | 목록 한 줄을 동일한 형식으로 출력 |
| 공용 | `input_nonempty()` | 빈 입력을 막고 값을 받을 때까지 반복 |
| 공용 | `choose_category()` | 카테고리 선택 또는 직접 입력 처리 |
| 공용 | `select_index()` | 번호 입력을 검증해 리스트 인덱스로 변환 |
| 화면 | `show_menu()` | 메뉴 출력 |
| 기능 | `add_prompt()` | 프롬프트 추가 |
| 기능 | `show_list()` | 전체 목록 출력 |
| 기능 | `show_by_category()` | 카테고리별 조회 |
| 기능 | `search_prompt()` | 키워드 검색 |
| 기능 | `show_detail()` | 상세 정보 출력 |
| 기능 | `toggle_favorite()` | 즐겨찾기 등록·해제 |
| 기능 | `show_favorites()` | 즐겨찾기 목록 출력 |
| 제어 | `main()` | 메뉴 루프와 분기 처리 |

총 14개 함수로 구성했습니다. `input_nonempty()` 와 `select_index()` 를 따로 만든 덕분에, 새 기능을 추가할 때마다 입력 검증 코드를 다시 쓰지 않고 함수 호출 한 줄로 해결할 수 있었습니다. 목록·카테고리 조회·검색·즐겨찾기 목록 네 기능이 모두 `print_prompt_line()` 하나를 공유하므로, 출력 모양을 바꿀 때 한 곳만 고치면 전체에 반영됩니다.

---

## 5. 데이터 설계 — 리스트와 딕셔너리

### 5-1. 쉽게 이해하기 : 서랍장과 서류봉투

프로그램이 데이터를 담는 방식을 사무실 서류 정리에 비유하면 이해가 쉽습니다.

**딕셔너리는 서류봉투입니다.** 봉투 겉면에 "제목", "내용", "카테고리", "즐겨찾기" 네 개의 칸이 인쇄되어 있고, 각 칸에 해당 정보를 적습니다. 무엇이 어디 적혀 있는지 겉면만 봐도 알 수 있습니다. 프롬프트 **한 건**이 봉투 하나입니다.

**리스트는 서랍장입니다.** 서랍이 1번, 2번, 3번 순서대로 붙어 있고 그 안에 봉투를 하나씩 넣습니다. "3번 서랍 꺼내 줘"라고 하면 항상 같은 봉투가 나옵니다. 프롬프트 **여러 건**을 담는 것이 서랍장입니다.

즉 이 프로그램의 데이터는 **"번호가 붙은 서랍장(리스트) 안에, 항목별 이름표가 붙은 서류봉투(딕셔너리)가 여러 개 들어 있는 구조"** 입니다.

| 비유 | 실제 | 하는 일 |
|---|---|---|
| 서랍장 | 리스트 `prompts` | 여러 건을 순서대로 보관 |
| 서랍 번호 | 인덱스 | "3번 프롬프트"처럼 지목하는 수단 |
| 서류봉투 | 딕셔너리 `{...}` | 프롬프트 한 건 |
| 봉투 겉면 칸 이름 | 키 `"title"` 등 | 어떤 정보인지 알려 주는 이름표 |
| 칸에 적힌 내용 | 값 | 실제 데이터 |

### 5-2. 실제 구조

```python
prompts = [                                    # ← 리스트 : 서랍장
    {                                          # ← 딕셔너리 : 서류봉투 한 개
        "title": "브랜드 광고 카피 작성",
        "content": "당신은 15년 경력의 카피라이터입니다. ...",
        "category": "텍스트 생성",
        "favorite": True,
    },
    { ... },  # 동화풍 캐릭터 키비주얼 생성
    { ... },  # 10초 브랜드 광고 스토리보드
    { ... },  # 메일 자동 분류 규칙 설계
]
```

| 키 | 자료형 | 의미 |
|---|---|---|
| `title` | 문자열 | 프롬프트 제목 |
| `content` | 문자열 | 프롬프트 본문 |
| `category` | 문자열 | 소속 카테고리 |
| `favorite` | 불리언 | 즐겨찾기 여부 (True / False) |

### 5-3. 왜 둘을 섞어 썼는가

두 자료형은 서로 다른 문제를 해결하기 때문에 하나만으로는 부족합니다.

**리스트만 쓴다면** 프롬프트 한 건을 `["브랜드 광고 카피 작성", "당신은...", "텍스트 생성", True]` 처럼 저장하게 됩니다. 이때 코드에 `prompt[2]` 라고 적히면 이게 카테고리인지 즐겨찾기인지 매번 손가락으로 세어 봐야 합니다. 순서를 하나만 바꿔도 프로그램 전체가 어긋납니다.

**딕셔너리만 쓴다면** 이름표는 생기지만 "몇 번째"라는 개념이 흐려집니다. 목록에 1, 2, 3 번호를 붙이고 사용자가 그 번호로 고르는 방식이 어려워집니다.

그래서 **바깥은 순서가 필요하니 리스트, 안쪽은 이름표가 필요하니 딕셔너리**를 썼습니다. 이 조합 덕분에 코드가 `prompts[2]["category"]` 처럼 읽히고, "세 번째 프롬프트의 카테고리"라는 뜻이 그대로 드러납니다.

### 5-4. 기능별 데이터 처리 방식

| 기능 | 하는 일 | 코드 형태 |
|---|---|---|
| 추가 | 서랍장 맨 뒤에 새 봉투 넣기 | `prompts.append({...})` |
| 목록 | 서랍을 1번부터 차례로 열어 보기 | `for i, p in enumerate(prompts, 1)` |
| 카테고리 조회 | 조건에 맞는 봉투만 골라내기 | `[p for p in prompts if p["category"] == 선택값]` |
| 검색 | 봉투의 두 칸을 동시에 훑기 | `if kw in p["title"].lower() or kw in p["content"].lower()` |
| 상세 보기 | 지정한 서랍의 봉투를 꺼내 칸별로 읽기 | `prompts[index]["title"]` |
| 즐겨찾기 | 봉투의 한 칸만 참↔거짓으로 뒤집기 | `prompts[i]["favorite"] = not prompts[i]["favorite"]` |
| 즐겨찾기 목록 | 그 칸이 참인 봉투만 모으기 | `[p for p in prompts if p["favorite"]]` |

`enumerate(prompts, 1)` 은 서랍을 순서대로 열면서 1번부터 번호를 함께 붙여 주는 문법입니다. 화면에는 1번부터 보여 주고 프로그램 내부에서는 0번부터 세기 때문에, `select_index()` 에서 입력값에 1을 빼 변환합니다. 검색에 `.lower()` 를 적용한 것은 대소문자를 구분하지 않고 찾기 위한 처리입니다.

### 5-5. 장점과 한계

**장점**

리스트와 딕셔너리 조합은 외부 라이브러리나 데이터베이스 없이도 구조화된 데이터를 다룰 수 있게 해 줍니다. 항목을 추가·삭제하는 비용이 낮고, 새 속성(예: 사용 횟수)이 필요해지면 딕셔너리에 이름표 하나만 더 붙이면 되어 기존 코드를 고치지 않아도 됩니다. 또한 딕셔너리 구조는 JSON 형식과 사실상 같은 모양이라, 나중에 파일 저장 기능을 붙일 때 변환 작업이 거의 필요 없습니다.

**한계**

첫째, 데이터가 메모리에만 존재하므로 프로그램을 종료하면 추가한 내용이 사라집니다. 이번 과제는 실행 중 유지가 요건이라 충족하지만, 실사용을 위해서는 파일 저장이 필요합니다. 둘째, 검색이 서랍을 처음부터 끝까지 하나씩 열어 보는 방식이라 데이터가 수만 건으로 늘면 속도가 떨어집니다. 셋째, 중간 항목을 삭제하면 뒤 항목의 번호가 전부 밀려 사용자가 기억한 번호와 달라집니다. 넷째, 딕셔너리 이름표를 `"favorite"` 대신 `"favorit"` 처럼 오타 내면 실행 시점에야 오류가 드러납니다.

이번 과제 규모(수십 건, 메모리 처리)에서는 성능보다 **코드 가독성과 확장성**이 중요하다고 판단해 이 조합을 선택했습니다. 위 한계들은 JSON 파일 저장, 고유 ID 부여, 클래스 도입 등으로 개선할 수 있으며 보고서 마지막에 정리했습니다.

---

## 6. 기능 구현 및 실행 검증

### 6-1. 메뉴 화면

```text
=== 나만의 프롬프트 관리 ===
1. 프롬프트 추가
2. 프롬프트 목록
3. 카테고리별 조회
4. 프롬프트 검색
5. 프롬프트 상세 보기
6. 즐겨찾기 관리
7. 즐겨찾기 목록
0. 종료
선택:
```

카테고리는 텍스트 생성, 이미지 생성, 영상 생성, 페르소나, 자동화, 기타 여섯 가지를 기본 제공하며, 선택 화면에서 `0` 을 누르면 목록에 없는 이름을 직접 입력할 수도 있습니다.

> 📎 `images/06-menu.png`

### 6-2. 프롬프트 추가 (메뉴 1)

제목과 내용, 카테고리를 입력받아 리스트에 추가합니다. 제목 입력란에서 아무것도 입력하지 않고 엔터를 누르면 다음과 같이 재입력을 요청하며, 이는 예외 처리가 작동하는 증빙입니다.

```text
=== 프롬프트 추가 ===
제목: 
입력값이 비어 있습니다. 다시 입력해 주세요.
제목: 나레이션 TTS 대본 작성
내용: 10초 브랜드 광고에 넣을 나레이션을 따뜻한 톤으로 25자 내외로 작성
(카테고리 선택 → 3. 영상 생성)

프롬프트가 추가되었습니다!
```

> 📎 `images/07-add.png`

### 6-3. 프롬프트 목록 (메뉴 2)

```text
=== 프롬프트 목록 ===
1. [텍스트 생성] 브랜드 광고 카피 작성 ⭐
2. [이미지 생성] 동화풍 캐릭터 키비주얼 생성
3. [영상 생성] 10초 브랜드 광고 스토리보드
4. [자동화] 메일 자동 분류 규칙 설계

총 4개의 프롬프트
```

`⭐` 표시는 즐겨찾기로 등록된 항목입니다. 목록이 비어 있을 경우에는 빈 화면 대신 "등록된 프롬프트가 없습니다." 안내를 출력합니다.

### 6-4. 카테고리별 조회 (메뉴 3)

**결과가 있는 경우**

```text
카테고리 선택: 1

[텍스트 생성] 카테고리 프롬프트
1. [텍스트 생성] 브랜드 광고 카피 작성 ⭐

총 1개
```

**결과가 없는 경우 — 예외 처리 확인**

```text
카테고리 선택: 4

'페르소나' 카테고리에 등록된 프롬프트가 없습니다.
```

> 📎 `images/11.images-category(1).png`, `images/11-images-category(2).png`

### 6-5. 프롬프트 검색 (메뉴 4)

**검색 성공** — 제목과 내용을 함께 검사하므로 "광고"로 두 건이 걸립니다.

```text
=== 프롬프트 검색 ===
검색어: 광고
1. [텍스트 생성] 브랜드 광고 카피 작성 ⭐
2. [영상 생성] 10초 브랜드 광고 스토리보드

검색 결과 2개
```

**검색 실패 — 예외 처리 확인**

```text
검색어: zzz

'zzz' 검색 결과가 없습니다.
```

> 📎 `images/12-search.png`

### 6-6. 프롬프트 상세 보기 (메뉴 5)

목록을 먼저 보여 준 뒤 번호를 선택받아, 제목·카테고리·즐겨찾기 여부·전체 내용을 출력합니다.

```text
상세히 볼 번호: 1
----------------------------------------
제목    : 브랜드 광고 카피 작성
카테고리: 텍스트 생성
즐겨찾기: 예
내용    : 당신은 15년 경력의 카피라이터입니다. 사회적기업 브랜드의
          10초 광고에 쓸 한 문장 핵심 메시지를 3개 제안해 주세요.
----------------------------------------
```

목록에서는 제목만 보이지만 상세 화면에서는 본문 전체를 확인할 수 있어, 프롬프트를 복사해 바로 사용할 수 있습니다.

> 📎 `images/13-detail.png`

### 6-7. 즐겨찾기 관리 및 목록 (메뉴 6, 7)

같은 번호를 다시 선택하면 해제되는 **토글 방식**으로 구현했습니다.

```text
즐겨찾기 변경할 번호: 2

'동화풍 캐릭터 키비주얼 생성' 즐겨찾기를 등록했습니다.
```

```text
=== 즐겨찾기 목록 ===
1. [텍스트 생성] 브랜드 광고 카피 작성 ⭐
2. [이미지 생성] 동화풍 캐릭터 키비주얼 생성 ⭐

총 2개
```

> 📎 `images/13.favolite.png`

### 6-8. 예외 처리 정리

| 상황 | 처리 결과 |
|---|---|
| 메뉴에 없는 번호(예: `9`) 입력 | "잘못된 번호입니다. 메뉴에 있는 번호를 입력해 주세요." 출력 후 메뉴 재표시 |
| 제목·내용을 비워 둔 채 엔터 | "입력값이 비어 있습니다." 출력 후 재입력 요청 |
| 숫자가 아닌 값을 번호로 입력 | "숫자를 입력해 주세요." 출력 후 메뉴 복귀 |
| 범위를 벗어난 번호 입력 | "존재하지 않는 번호입니다." 출력 후 메뉴 복귀 |
| 목록이 빈 상태에서 상세·즐겨찾기 시도 | 즉시 복귀해 인덱스 오류 방지 |
| 조회·검색 결과 없음 | 빈 화면 대신 안내 문구 출력 |
| 잘못된 카테고리 번호 | '기타'로 자동 지정 후 안내 |

어떤 잘못된 입력에도 프로그램이 강제 종료되지 않고 메뉴로 돌아오는 것을 실행 과정에서 확인했습니다.

---

## 7. 버전 관리 수행 과정

### 7-1. 커밋 전략

기능 한 덩어리가 완성되어 정상 동작을 확인한 시점마다 커밋했습니다. 메시지는 접두어 규칙을 적용해 이력만 보고도 성격을 알 수 있게 했습니다.

| 접두어 | 용도 |
|---|---|
| `feat` | 새로운 기능 추가 |
| `fix` | 동작 오류·누락 보완 |
| `docs` | 문서 작성 및 수정 |
| `chore` | 설정 파일·환경 관련 작업 |
| `merge` | 브랜치 병합 |

이번 프로젝트에서는 위 다섯 가지를 실제로 사용했습니다.

### 7-2. 브랜치 생성과 병합

프롬프트 목록 기능은 `main` 을 건드리지 않고 별도 브랜치에서 개발한 뒤 병합했습니다.

```bash
git checkout -b feature/prompt-list      # 브랜치 생성 및 이동
# ... 목록 기능 구현 및 2건 커밋 ...
git checkout main                        # 원래 줄기로 복귀
git merge --no-ff feature/prompt-list -m "merge: 프롬프트 목록 기능 main 병합"
git push                                 # main 반영
git push origin feature/prompt-list      # 원격에도 브랜치 반영
```

`--no-ff` 옵션을 사용한 이유는 병합 이력을 남기기 위해서입니다. 이 옵션이 없으면 Git이 커밋을 일렬로 이어붙여, 나중에 이력을 보아도 브랜치 작업이 있었다는 사실이 드러나지 않습니다.

또한 로컬에만 있던 브랜치를 `git push origin feature/prompt-list` 로 원격에 올려, GitHub 저장소 상단에 **2 Branches** 로 표시되도록 했습니다. 커밋 이력에도 `origin/feature/prompt-list` 가 함께 표시됩니다.

### 7-3. pull 실습

`pull` 은 원격의 변경을 로컬로 가져오는 명령입니다. 로컬이 최신인 상태에서는 가져올 것이 없으므로, GitHub 웹 편집기에서 `README.md` 에 한 줄을 추가해 원격에만 변경을 만든 뒤 내려받았습니다.

```text
From https://github.com/kwork0828/A1-1
   e127a76..d638392  main       -> origin/main
Updating e127a76..d638392
Fast-forward
 README.md | 1 +
 1 file changed, 1 insertion(+)
```

`Fast-forward` 는 로컬에 충돌될 변경이 없어 원격 내용을 그대로 이어붙였다는 뜻입니다.

> 📎 `images/15-gitpull.png`

### 7-4. 최종 커밋 이력 (총 17건)

```text
*   1ca9ead (HEAD -> main, origin/main) Merge branch 'main' of https://github.com/kwork0828/A1-1
|\
| * aa7238f Create report.md
* | 9b46bc4 docs: 과제 결과 보고서 추가
|/
* 1bdeac1 docs: README 문서 보강
* d638392 Update README.md
* e127a76 docs:프로그램 설명, 실행방법, 기능목록 작성
* ad19855 feat: 검색, 상세보기, 즐겨찾기 기능 구현
* 0673dfa feat:카테고리별 조회 기능 구현
*   6852aa1 merge: 프롬프트 목록 기능 main 병합
|\
| * 042ab4f (origin/feature/prompt-list, feature/prompt-list) fix: 프롬프트가 없을 때 안내 메시지 출력
| * 28bb833 feat: 프롬프트 목록 출력 기능 구현
|/
* 8f4798b feat: 공통 유틸 함수 분리 및 프롬프트 추가 기능 구현
* f72b953 feat: 메뉴 출력 함수 및 메인 루프 구현 (잘못된 입력 처리 포함)
* d48f554 feat: 기본 프롬프트 데이터 4개 및 카테고리 상수 추가
* 282a12d docs: README 프로젝트 제목 작성
* 49d8332 chore: .gitignore 추가 및 환경설정 스크린샷 정리
* 426299b fist commit
```

이 그래프에는 **두 번의 병합**이 기록되어 있습니다.

첫 번째 `6852aa1` 은 과제 요건인 **기능 브랜치 병합**입니다. `28bb833` 과 `042ab4f` 두 커밋이 `feature/prompt-list` 에서 이루어진 뒤 `--no-ff` 로 `main` 에 합쳐졌고, `|\` 로 갈라져 `|/` 로 다시 모이는 모양이 그 증거입니다.

두 번째 `1ca9ead` 는 **원격과 로컬의 동기화 병합**입니다. GitHub 웹에서 `report.md` 를 만든 커밋(`aa7238f`)과 로컬에서 작업한 커밋(`9b46bc4`)이 각각 존재해 `push` 가 거절되었고, `git pull` 로 두 흐름을 합쳐 해결했습니다. 실제 협업에서 자주 발생하는 상황을 그대로 겪고 처리한 사례입니다.

첫 커밋 메시지 `fist commit` 은 `first` 의 오타입니다. 이미 원격에 올라간 이력을 수정하면 이후 모든 커밋 해시가 바뀌므로 그대로 두었고, 대신 이후 커밋부터 접두어 규칙을 정해 적용했습니다.

> 📎 `images/16-graph.png`

### 7-5. 사용한 Git 명령

| 명령 | 사용 목적 |
|---|---|
| `git init` | 로컬 저장소 초기화 |
| `git clone` | GitHub 저장소를 로컬로 복제 (작업 시작) |
| `git add` | 변경 파일을 커밋 대상으로 등록 |
| `git commit` | 변경 내용을 이력으로 확정 |
| `git push` | 로컬 커밋을 GitHub에 업로드 |
| `git pull` | GitHub의 변경 사항을 로컬로 반영 |
| `git checkout` | 브랜치 생성 및 전환 |
| `git merge` | 브랜치를 `main` 에 병합 |
| `git status` | 현재 변경 상태 확인 (작업 전후 수시) |
| `git log --graph` | 커밋 이력과 브랜치 흐름 확인 |

---

## 8. 시행착오와 해결

과정에서 겪은 문제와 원인, 해결 방법을 기록합니다.

| 문제 | 원인 | 해결 |
|---|---|---|
| `fatal: not a git repository` | 터미널이 프로젝트 폴더가 아닌 다른 경로에 있었음 | `cd` 로 이동, 이후 VSCode에서 폴더를 직접 열어 사용 |
| 코드를 고쳤는데 반영되지 않음 | 저장하지 않은 상태로 실행 | 실행 전 `Ctrl+S` 저장 및 탭의 미저장 표시(●) 확인 |
| Git 명령이 "잘못된 번호입니다"로 처리됨 | 프로그램 실행 중(`선택:` 상태)에 명령을 입력 | `0` 으로 종료해 `PS ...>` 프롬프트를 확인한 뒤 입력 |
| 터미널이 `>>` 상태로 멈춤 | 커밋 메시지의 따옴표 짝이 맞지 않음 | `Ctrl+C` 로 중단, 메시지를 직접 타이핑 |
| 터미널이 `:` 상태로 멈춰 입력 불가 | `git log` 가 페이저 모드로 열림 | `q` 로 종료, 이후 `git --no-pager log` 사용 |
| README 수정이 GitHub에 반영되지 않음 | 저장 전 커밋으로 일부 줄만 반영 | 저장 후 `git status` 로 `modified` 확인하고 재커밋 (148줄 반영 확인) |
| `git push` 거절됨 | GitHub 웹에서 파일을 만들어 원격에만 있는 커밋 발생 | `git pull` 로 병합 후 push. 이후 "웹에서 수정했으면 pull 먼저" 원칙 적용 |
| 병합 시 낯선 편집기 화면 출현 | 병합 메시지 입력용 Vim 편집기가 열림 | `Esc` 후 `:wq` 로 저장·종료 |

이 과정에서 얻은 원칙은 **"저장 → 프로그램 종료 → add → commit → push"** 순서를 지키는 것, 커밋 후 `git status` 와 변경 줄 수로 반영 여부를 반드시 확인하는 것, 그리고 웹과 로컬을 함께 수정했다면 반드시 `pull` 을 먼저 하는 것입니다.

---

## 9. 요건 충족 체크리스트

### 9-1. 프로그램 요건 (15항목)

| 확인 | 요건 | 충족 근거 |
|:--:|---|---|
| ✅ | 파이썬 콘솔 프로그램 | `prompt_manager.py`, 터미널 실행 확인 |
| ✅ | 메뉴 방식 인터페이스 | 1~7번 + 0번 종료, `show_menu()` |
| ✅ | 반복 실행 구조 | `main()` 의 `while True` 루프 |
| ✅ | 함수 단위 기능 분리 | 총 14개 함수, 공용 함수 5개 분리 |
| ✅ | 프롬프트 추가 | `add_prompt()` — 6-2 실행 화면 |
| ✅ | 프롬프트 목록 | `show_list()` — 6-3 실행 화면 |
| ✅ | 카테고리별 조회 | `show_by_category()` — 6-4 실행 화면 |
| ✅ | 키워드 검색 | `search_prompt()` — 6-5 실행 화면 |
| ✅ | 상세 보기 | `show_detail()` — 6-6 실행 화면 |
| ✅ | 즐겨찾기 기능 | `toggle_favorite()`, `show_favorites()` — 6-7 실행 화면 |
| ✅ | 리스트 활용 | `prompts` 리스트, `append`·순회·컴프리헨션 |
| ✅ | 딕셔너리 활용 | 4개 키를 가진 딕셔너리 구조 |
| ✅ | 기본 데이터 3건 이상 | 4건 사전 등록 (요건 초과) |
| ✅ | 실행 중 데이터 유지 | 추가한 프롬프트가 목록·검색에 즉시 반영 |
| ✅ | 잘못된 입력 예외 처리 | 7가지 상황 대응, 6-8 표 참조 |

### 9-2. 버전 관리 요건 (12항목)

| 확인 | 요건 | 충족 근거 |
|:--:|---|---|
| ✅ | GitHub 저장소 생성 | https://github.com/kwork0828/A1-1 |
| ✅ | `git config` 사용자 정보 설정 | 3-1 설정 화면 |
| ✅ | 의미 있는 커밋 10건 이상 | **17건**, 접두어 규칙 적용 |
| ✅ | 기능 단위 커밋 분할 | 데이터 → 메뉴 → 유틸 → 기능별 순차 커밋 |
| ✅ | 브랜치 생성 | `feature/prompt-list` |
| ✅ | 브랜치 병합 | `merge --no-ff`, 그래프에 `\|\` `\|/` 확인 |
| ✅ | 원격 브랜치 반영 | GitHub 저장소 **2 Branches** 표시 |
| ✅ | init·add·commit·push 사용 | 전 과정에서 사용 |
| ✅ | clone 사용 | 저장소를 로컬로 복제해 작업 시작 |
| ✅ | pull 사용 | 웹 수정 후 `Fast-forward` 확인 |
| ✅ | 충돌 상황 처리 경험 | 원격·로컬 분기 발생 후 pull 병합으로 해결 |
| ✅ | `.gitignore` 설정 | 캐시·설정 파일 제외 |

### 9-3. 문서 및 제출물 요건 (9항목)

| 확인 | 요건 | 충족 근거 |
|:--:|---|---|
| ✅ | README 프로그램 설명 | 제작 배경, 개발 환경 항목 |
| ✅ | README 실행 방법 | clone·실행 명령, 한글 깨짐 대응 안내 |
| ✅ | README 기능 목록 | 8개 메뉴 표 정리 |
| ✅ | README 데이터 구조 설명 | 딕셔너리 구조 및 키 설명 표 |
| ✅ | 결과 보고서 작성 | 본 문서 `report.md` |
| ✅ | ① 저장소 URL 제출 | 보고서 상단 명기 |
| ✅ | ② 개발 환경 스크린샷 | `images/01`, `02-연동`, `03.python version` |
| ✅ | ③ 실행 결과 스크린샷 | `06-menu`, `07-add`, `11.images-category(1)`, `11-images-category(2)`, `12-search`, `13-detail`, `13.favolite` |
| ✅ | ④ 커밋 그래프 스크린샷 | `images/16-graph.png` |

**총 36개 항목 전체 충족.**

---

## 10. 마무리

### 배운 점

기능을 잘게 쪼개어 하나씩 완성하고 커밋하는 방식이 실제로 유효했습니다. 오류가 생겼을 때 직전 커밋과 비교하면 원인 범위가 좁아져 수정이 빨랐습니다. 또한 브랜치를 나눠 작업하면 완성 전 코드가 `main` 을 오염시키지 않는다는 점, 병합 이력을 남겨 두면 작업 맥락이 기록으로 보존된다는 점을 확인했습니다.

특히 원격과 로컬에서 동시에 파일을 수정해 `push` 가 거절된 상황은, Git이 왜 "먼저 가져오고 나중에 올리는" 순서를 강제하는지 이해하게 된 계기였습니다. 명령을 외우는 것보다 저장소의 상태를 그림으로 떠올리는 것이 문제 해결에 더 도움이 되었습니다.

데이터 설계 측면에서는 자료형 선택이 코드 가독성에 직접 영향을 준다는 것을 체감했습니다. 딕셔너리의 키 이름이 곧 문서 역할을 하기 때문에, 시간이 지나 코드를 다시 읽어도 의미를 파악하기 쉬웠습니다.

### 개선 방향

| 항목 | 내용 | 기대 효과 |
|---|---|---|
| 데이터 영속화 | JSON 파일 저장·불러오기 | 종료 후에도 데이터 보존 |
| 수정·삭제 기능 | 등록한 프롬프트를 고치거나 지우는 메뉴 | 관리 기능 완성 |
| 중복 방지 | 고유 ID 부여 및 동일 제목 등록 시 확인 | 데이터 품질 확보 |
| 사용 통계 | 조회 횟수 기록 후 자주 쓰는 순 정렬 | 실사용 편의 향상 |
| 구조 개선 | 클래스를 도입해 데이터와 동작을 함께 묶기 | 키 오타 방지, 형식 강제 |
| 검색 강화 | 카테고리와 키워드 동시 조건 검색 | 데이터 증가 시 유용 |
